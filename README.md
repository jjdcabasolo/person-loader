# Person Loader
React app that loads a person's details 2 at a time. It uses Next.js, Chakra UI, react-query, as well as Axios and req|res endpoints.

# Quickstart

To run the app locally:

```sh
# requires node 10 & npm 6 or higher

yarn install

yarn dev
```
